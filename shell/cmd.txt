
Usefulle cmd 

- Search a file/directory or executable:

`find` for files and directories
`which` for executable

- Difference between two files

`diff` file1 file2

In a git repo:

`git diff` file1 file2
